#take 5 values from the user and write a program that prints the values you get on the screen. Print the type of values you received in this program on the screen.
When using print functions, don't forget to use f-string and format usage in your program

a, b, x, y, z = input("Enter a five value: ").split()
print("Numbers you entered: ", a, b, x, y, z)                   # One version
print(f"Numbers you entered: {a}, {b}, {x}, {y}, {z}.")         # Anotheer version
